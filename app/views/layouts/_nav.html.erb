<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">Loaction Share</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item <%= controller.controller_name == 'locations' ? 'active' : ''%>">
        <!-- <a class="nav-link" href="#"> My Link </a> -->
        <%= link_to "My Location's", my_location_path, class: "nav-link" %>
      </li>

      <li class="nav-item <%= controller.controller_name == 'locations' ? 'active' : ''%>">
        <%= link_to "New Location", new_location_path, class: "nav-link" %> 
      </li>

      <li class="nav-item <%= controller.controller_name == 'locations' ? 'active' : ''%>">
        <%= link_to "Other's Location", others_location_path, class: "nav-link" %> 
      </li>
         </ul>
      <% if user_signed_in? %>
            <ul class="nav-item"> <%= link_to "Account Setting", edit_user_registration_path %></ul>
            <ul class="nav-item"> <%= link_to "Logout", destroy_user_session_path, method: :delete %></ul>
      <% end %>
 </div>
</nav>


